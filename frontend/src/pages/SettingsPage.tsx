import { useState, type ReactNode } from 'react'
import { Bell, Check, Database, Globe2, KeyRound, Languages, Moon, Save, ShieldCheck, SlidersHorizontal, Volume2 } from 'lucide-react'
import { Button, Card, CardHeader, DataLabel, Pill } from '../components/ui'
import { apiState } from '../services/api'

export function SettingsPage() {
  const [demo, setDemo] = useState(() => localStorage.getItem('agronova-demo-mode') !== 'false')
  const [voice, setVoice] = useState(() => localStorage.getItem('agronova-voice') !== 'false')
  const [alerts, setAlerts] = useState(() => localStorage.getItem('agronova-alerts') !== 'false')
  const [language, setLanguage] = useState('English')
  const [saved, setSaved] = useState(false)
  const toggleDemo = () => { const next = !demo; setDemo(next); apiState.setDemoMode(next); localStorage.setItem('agronova-demo-mode', String(next)) }
  const save = () => { localStorage.setItem('agronova-voice', String(voice)); localStorage.setItem('agronova-alerts', String(alerts)); setSaved(true); window.setTimeout(() => setSaved(false), 1800) }
  return <div className="space-y-6"><div className="flex flex-wrap items-end justify-between gap-4"><div><div className="mb-2 flex items-center gap-2 text-xs font-semibold text-slate-500"><SlidersHorizontal size={15} />Workspace controls</div><h1 className="text-2xl font-extrabold tracking-tight text-ink sm:text-3xl">Settings</h1><p className="mt-1 text-sm text-slate-500">Configure the prototype experience without exposing credentials in the browser.</p></div><Button onClick={save}>{saved ? <><Check size={15} />Saved</> : <><Save size={15} />Save preferences</>}</Button></div><div className="grid gap-6 lg:grid-cols-2"><Card><CardHeader eyebrow="Demo experience" title="Data and notifications" /><div className="divide-y divide-slate-100"><Setting icon={<Database size={17} />} title="Demo Mode" description="Use the local synthetic fallback when the API is unavailable." enabled={demo} onChange={toggleDemo} /><Setting icon={<Bell size={17} />} title="Automatic alerts" description="Show weather and crop-risk alerts in the workspace." enabled={alerts} onChange={() => setAlerts(!alerts)} /><Setting icon={<Volume2 size={17} />} title="Voice advisory" description="Enable browser speech playback for advisories." enabled={voice} onChange={() => setVoice(!voice)} /></div><div className="m-5 rounded-xl bg-amber-50 p-3 text-[10px] leading-5 text-amber-800"><strong>Data notice:</strong> Demo values are synthetic and are not government observations or validated forecasts.</div></Card><Card><CardHeader eyebrow="Presentation" title="Language and display" /><div className="space-y-5 p-5"><label className="block text-[11px] font-bold text-slate-500">Interface language<div className="relative mt-1.5"><Languages size={15} className="absolute left-3 top-3 text-slate-400" /><select value={language} onChange={(event) => { const next = event.target.value === 'தமிழ் (Tamil)' ? 'ta' : 'en'; setLanguage(next === 'ta' ? 'தமிழ் (Tamil)' : 'English'); localStorage.setItem('agronova-language', next); window.dispatchEvent(new CustomEvent('agronova-language', { detail: next })) }} className="field-input pl-9"><option>English</option><option>தமிழ் (Tamil)</option></select></div></label><div className="grid grid-cols-2 gap-3"><Preference icon={<Globe2 size={16} />} label="Units" value="Metric (°C, mm)" /><Preference icon={<Moon size={16} />} label="Theme" value="Light / agriculture" /></div><div className="rounded-xl bg-slate-50 p-4 text-xs leading-5 text-slate-500">Language switching is wired to the workspace header. Tamil browser voices are used when available on the device.</div></div></Card><Card><CardHeader eyebrow="API connection" title="Prototype services" /><div className="space-y-3 p-5"><Connection label="FastAPI endpoint" value={import.meta.env.VITE_API_URL || '/api'} status="Configurable" /><Connection label="Weather provider" value="DemoWeatherProvider" status="No API key required" /><Connection label="Model registry" value="prototype-rf-v1" status="Loaded on demand" /><div className="flex gap-2 rounded-xl bg-blue-50 p-3 text-[10px] leading-5 text-blue-800"><ShieldCheck size={14} className="mt-0.5 shrink-0" />API keys belong in backend environment variables only. Never place provider secrets in VITE_* variables.</div></div></Card><Card><CardHeader eyebrow="About" title="AgroNova AI" /><div className="space-y-4 p-5"><div className="flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-forest text-white"><Database size={21} /></div><div><div className="text-sm font-bold text-ink">Panchayat intelligence prototype</div><div className="mt-1 text-[10px] text-slate-400">Version 0.1.0 · SIH demonstration build</div></div></div><p className="text-xs leading-6 text-slate-500">AgroNova AI translates coarse block weather into explainable, crop-specific decision support through spatial features and a replaceable ML model.</p><DataLabel /></div></Card></div></div>
}
function Setting({ icon, title, description, enabled, onChange }: { icon: ReactNode; title: string; description: string; enabled: boolean; onChange: () => void }) { return <div className="flex items-center gap-3 px-5 py-4"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-slate-100 text-slate-500">{icon}</div><div className="min-w-0 flex-1"><div className="text-xs font-bold text-ink">{title}</div><div className="mt-1 text-[10px] leading-4 text-slate-400">{description}</div></div><button onClick={onChange} className={`relative h-6 w-10 rounded-full transition ${enabled ? 'bg-forest' : 'bg-slate-200'}`}><span className={`absolute top-1 h-4 w-4 rounded-full bg-white shadow-sm transition ${enabled ? 'left-5' : 'left-1'}`} /></button></div> }
function Preference({ icon, label, value }: { icon: ReactNode; label: string; value: string }) { return <div className="rounded-xl bg-slate-50 p-3"><div className="flex items-center gap-1.5 text-[10px] font-bold text-slate-400">{icon}{label}</div><div className="mt-2 text-xs font-bold text-ink">{value}</div></div> }
function Connection({ label, value, status }: { label: string; value: string; status: string }) { return <div className="flex flex-wrap items-center gap-2 rounded-xl border border-slate-100 p-3"><KeyRound size={15} className="text-leaf" /><div className="min-w-0 flex-1"><div className="text-[10px] font-bold uppercase tracking-wider text-slate-400">{label}</div><div className="mt-1 truncate text-xs font-semibold text-ink">{value}</div></div><Pill tone="green">{status}</Pill></div> }
